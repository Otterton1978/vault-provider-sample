provider vault {
    # Use VAULT_ADDR as an environment variable to specify the address of vault server
    # Use VAULT_TOKEN as an environment variable to specify the token to be used
}

provider vault {
    alias = "team_a"
    namespace = "team_a"
    # Use VAULT_ADDR as an environment variable to specify the address of vault server
    # Use VAULT_TOKEN as an environment variable to specify the token to be used
}